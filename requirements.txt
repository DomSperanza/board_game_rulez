# NumPy 2.x wheels need x86-64-v2; pin 1.x for older VPS / emulated CPUs
numpy>=1.26.4,<2

# PDF Processing
pymupdf==1.25.3  # Fast PDF text extraction

# Vector Database & Embeddings
chromadb==0.5.21  # Local vector database (stores embeddings free/locally)
posthog==4.8.0  # Pin: Chroma telemetry breaks with newer posthog (#4997)
sentence-transformers==3.3.1  # Runs open-source embedding models locally

# AI Integration
google-generativeai==0.8.3  # Gemini API for the free tier generative completion

# Configuration & Utilities
python-dotenv==1.0.1  # For loading GEMINI_API_KEY securely

# UI / Web
streamlit==1.40.1
flask==3.0.3
